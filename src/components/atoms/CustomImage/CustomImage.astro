---
import { Image } from 'astro:assets'

const { src = '', alt = '', additionalClasses = '', ...rest } = Astro.props

if (!src) {
  console.error('Image component requires a "src" attribute')
}
---

<Image src={src} alt={alt} class={'flex' + additionalClasses} {...rest} />
