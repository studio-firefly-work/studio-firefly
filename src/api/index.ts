import * as auth from '@/api/auth'
import * as mail from '@/api/mail'
import * as preUser from '@/api/preUser'
import * as user from '@/api/user'

export const api = {
  auth,
  mail,
  preUser,
  user,
}
